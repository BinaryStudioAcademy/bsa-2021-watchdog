/**
 * Use only to pdf/excel export, property is the table column
 */
export interface MemberExport {
    FullName: string,
    Email: string,
    Role: string,
    Teams: string,
    Accepted: string,
}
